<div class="container">
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<div class="container">
		<button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
		<a href="{{ path('option_index') }}" class="btn btn-secondary">back to list</a>
	</div>
   
{{ form_end(form) }}
</div>

